namespace TicTacToe.Enums {
    public enum Player {
        X,
        O
    }

    public enum GameMode {
        SinglePlayer,
        MultiPlayer
    }

    public enum Tile {
        Empty,
        X,
        O
    }
};